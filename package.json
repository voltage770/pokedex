{
  "name": "pokedex",
  "version": "1.0.0",
  "scripts": {
    "dev":      "cd app && npm run dev",
    "build":    "cd app && npm run build",
    "generate": "cd scripts && node db/generate.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
