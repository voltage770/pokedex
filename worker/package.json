{
  "name": "pokedex-news-worker",
  "version": "1.0.0",
  "private": true,
  "description": "cloudflare worker that fetches + parses pokemon news for the pokedex site",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.82.2"
  }
}
